"use strict";
var qt = Object.create;
var w = Object.defineProperty;
var It = Object.getOwnPropertyDescriptor;
var Lt = Object.getOwnPropertyNames;
var jt = Object.getPrototypeOf, Ft = Object.prototype.hasOwnProperty;
var l = (t, e) => () => (e || t((e = { exports: {} }).exports, e), e.exports), Ht = (t, e) => {
  for (var n in e)
    w(t, n, { get: e[n], enumerable: !0 });
}, $ = (t, e, n, r) => {
  if (e && typeof e == "object" || typeof e == "function")
    for (let s of Lt(e))
      !Ft.call(t, s) && s !== n && w(t, s, { get: () => e[s], enumerable: !(r = It(e, s)) || r.enumerable });
  return t;
};
var N = (t, e, n) => (n = t != null ? qt(jt(t)) : {}, $(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  e || !t || !t.__esModule ? w(n, "default", { value: t, enumerable: !0 }) : n,
  t
)), zt = (t) => $(w({}, "__esModule", { value: !0 }), t);

// node_modules/isexe/windows.js
var D = l((Pe, W) => {
  "use strict";
  W.exports = z;
  z.sync = Bt;
  var F = require("fs");
  function Mt(t, e) {
    var n = e.pathExt !== void 0 ? e.pathExt : process.env.PATHEXT;
    if (!n || (n = n.split(";"), n.indexOf("") !== -1))
      return !0;
    for (var r = 0; r < n.length; r++) {
      var s = n[r].toLowerCase();
      if (s && t.substr(-s.length).toLowerCase() === s)
        return !0;
    }
    return !1;
  }
  function H(t, e, n) {
    return !t.isSymbolicLink() && !t.isFile() ? !1 : Mt(e, n);
  }
  function z(t, e, n) {
    F.stat(t, function(r, s) {
      n(r, r ? !1 : H(s, t, e));
    });
  }
  function Bt(t, e) {
    return H(F.statSync(t), t, e);
  }
});

// node_modules/isexe/mode.js
var G = l((Oe, X) => {
  "use strict";
  X.exports = M;
  M.sync = Xt;
  var K = require("fs");
  function M(t, e, n) {
    K.stat(t, function(r, s) {
      n(r, r ? !1 : B(s, e));
    });
  }
  function Xt(t, e) {
    return B(K.statSync(t), e);
  }
  function B(t, e) {
    return t.isFile() && Gt(t, e);
  }
  function Gt(t, e) {
    var n = t.mode, r = t.uid, s = t.gid, o = e.uid !== void 0 ? e.uid : process.getuid && process.getuid(), i = e.gid !== void 0 ? e.gid : process.getgid && process.getgid(), u = parseInt("100", 8), c = parseInt("010", 8), a = parseInt("001", 8), f = u | c, p = n & a || n & c && s === i || n & u && r === o || n & f && o === 0;
    return p;
  }
});

// node_modules/isexe/index.js
var Y = l((Se, U) => {
  "use strict";
  var Ce = require("fs"), x;
  process.platform === "win32" || global.TESTING_WINDOWS ? x = D() : x = G();
  U.exports = _;
  _.sync = Ut;
  function _(t, e, n) {
    if (typeof e == "function" && (n = e, e = {}), !n) {
      if (typeof Promise != "function")
        throw new TypeError("callback not provided");
      return new Promise(function(r, s) {
        _(t, e || {}, function(o, i) {
          o ? s(o) : r(i);
        });
      });
    }
    x(t, e || {}, function(r, s) {
      r && (r.code === "EACCES" || e && e.ignoreErrors) && (r = null, s = !1), n(r, s);
    });
  }
  function Ut(t, e) {
    try {
      return x.sync(t, e || {});
    } catch (n) {
      if (e && e.ignoreErrors || n.code === "EACCES")
        return !1;
      throw n;
    }
  }
});

// node_modules/which/which.js
var nt = l((ke, et) => {
  "use strict";
  var g = process.platform === "win32" || process.env.OSTYPE === "cygwin" || process.env.OSTYPE === "msys", V = require("path"), Yt = g ? ";" : ":", J = Y(), Q = (t) => Object.assign(new Error(`not found: ${t}`), { code: "ENOENT" }), Z = (t, e) => {
    let n = e.colon || Yt, r = t.match(/\//) || g && t.match(/\\/) ? [""] : [
      // windows always checks the cwd first
      ...g ? [process.cwd()] : [],
      ...(e.path || process.env.PATH || /* istanbul ignore next: very unusual */
      "").split(n)
    ], s = g ? e.pathExt || process.env.PATHEXT || ".EXE;.CMD;.BAT;.COM" : "", o = g ? s.split(n) : [""];
    return g && t.indexOf(".") !== -1 && o[0] !== "" && o.unshift(""), {
      pathEnv: r,
      pathExt: o,
      pathExtExe: s
    };
  }, tt = (t, e, n) => {
    typeof e == "function" && (n = e, e = {}), e || (e = {});
    let { pathEnv: r, pathExt: s, pathExtExe: o } = Z(t, e), i = [], u = (a) => new Promise((f, p) => {
      if (a === r.length)
        return e.all && i.length ? f(i) : p(Q(t));
      let d = r[a], v = /^".*"$/.test(d) ? d.slice(1, -1) : d, h = V.join(v, t), y = !v && /^\.[\\\/]/.test(t) ? t.slice(0, 2) + h : h;
      f(c(y, a, 0));
    }), c = (a, f, p) => new Promise((d, v) => {
      if (p === s.length)
        return d(u(f + 1));
      let h = s[p];
      J(a + h, { pathExt: o }, (y, Nt) => {
        if (!y && Nt)
          if (e.all)
            i.push(a + h);
          else
            return d(a + h);
        return d(c(a, f, p + 1));
      });
    });
    return n ? u(0).then((a) => n(null, a), n) : u(0);
  }, Vt = (t, e) => {
    e = e || {};
    let { pathEnv: n, pathExt: r, pathExtExe: s } = Z(t, e), o = [];
    for (let i = 0; i < n.length; i++) {
      let u = n[i], c = /^".*"$/.test(u) ? u.slice(1, -1) : u, a = V.join(c, t), f = !c && /^\.[\\\/]/.test(t) ? t.slice(0, 2) + a : a;
      for (let p = 0; p < r.length; p++) {
        let d = f + r[p];
        try {
          if (J.sync(d, { pathExt: s }))
            if (e.all)
              o.push(d);
            else
              return d;
        } catch {
        }
      }
    }
    if (e.all && o.length)
      return o;
    if (e.nothrow)
      return null;
    throw Q(t);
  };
  et.exports = tt;
  tt.sync = Vt;
});

// node_modules/path-key/index.js
var st = l((Te, P) => {
  "use strict";
  var rt = (t = {}) => {
    let e = t.env || process.env;
    return (t.platform || process.platform) !== "win32" ? "PATH" : Object.keys(e).reverse().find((r) => r.toUpperCase() === "PATH") || "Path";
  };
  P.exports = rt;
  P.exports.default = rt;
});

// node_modules/cross-spawn/lib/util/resolveCommand.js
var at = l((Ae, ct) => {
  "use strict";
  var ot = require("path"), Jt = nt(), Qt = st();
  function it(t, e) {
    let n = t.options.env || process.env, r = process.cwd(), s = t.options.cwd != null, o = s && process.chdir !== void 0 && !process.chdir.disabled;
    if (o)
      try {
        process.chdir(t.options.cwd);
      } catch {
      }
    let i;
    try {
      i = Jt.sync(t.command, {
        path: n[Qt({ env: n })],
        pathExt: e ? ot.delimiter : void 0
      });
    } catch {
    } finally {
      o && process.chdir(r);
    }
    return i && (i = ot.resolve(s ? t.options.cwd : "", i)), i;
  }
  function Zt(t) {
    return it(t) || it(t, !0);
  }
  ct.exports = Zt;
});

// node_modules/cross-spawn/lib/util/escape.js
var ut = l((Re, C) => {
  "use strict";
  var O = /([()\][%!^"`<>&|;, *?])/g;
  function te(t) {
    return t = t.replace(O, "^$1"), t;
  }
  function ee(t, e) {
    return t = `${t}`, t = t.replace(/(\\*)"/g, '$1$1\\"'), t = t.replace(/(\\*)$/, "$1$1"), t = `"${t}"`, t = t.replace(O, "^$1"), e && (t = t.replace(O, "^$1")), t;
  }
  C.exports.command = te;
  C.exports.argument = ee;
});

// node_modules/shebang-regex/index.js
var pt = l(($e, lt) => {
  "use strict";
  lt.exports = /^#!(.*)/;
});

// node_modules/shebang-command/index.js
var ft = l((Ne, dt) => {
  "use strict";
  var ne = pt();
  dt.exports = (t = "") => {
    let e = t.match(ne);
    if (!e)
      return null;
    let [n, r] = e[0].replace(/#! ?/, "").split(" "), s = n.split("/").pop();
    return s === "env" ? r : r ? `${s} ${r}` : s;
  };
});

// node_modules/cross-spawn/lib/util/readShebang.js
var ht = l((qe, mt) => {
  "use strict";
  var S = require("fs"), re = ft();
  function se(t) {
    let n = Buffer.alloc(150), r;
    try {
      r = S.openSync(t, "r"), S.readSync(r, n, 0, 150, 0), S.closeSync(r);
    } catch {
    }
    return re(n.toString());
  }
  mt.exports = se;
});

// node_modules/cross-spawn/lib/parse.js
var wt = l((Ie, vt) => {
  "use strict";
  var oe = require("path"), gt = at(), Et = ut(), ie = ht(), ce = process.platform === "win32", ae = /\.(?:com|exe)$/i, ue = /node_modules[\\/].bin[\\/][^\\/]+\.cmd$/i;
  function le(t) {
    t.file = gt(t);
    let e = t.file && ie(t.file);
    return e ? (t.args.unshift(t.file), t.command = e, gt(t)) : t.file;
  }
  function pe(t) {
    if (!ce)
      return t;
    let e = le(t), n = !ae.test(e);
    if (t.options.forceShell || n) {
      let r = ue.test(e);
      t.command = oe.normalize(t.command), t.command = Et.command(t.command), t.args = t.args.map((o) => Et.argument(o, r));
      let s = [t.command].concat(t.args).join(" ");
      t.args = ["/d", "/s", "/c", `"${s}"`], t.command = process.env.comspec || "cmd.exe", t.options.windowsVerbatimArguments = !0;
    }
    return t;
  }
  function de(t, e, n) {
    e && !Array.isArray(e) && (n = e, e = null), e = e ? e.slice(0) : [], n = Object.assign({}, n);
    let r = {
      command: t,
      args: e,
      options: n,
      file: void 0,
      original: {
        command: t,
        args: e
      }
    };
    return n.shell ? r : pe(r);
  }
  vt.exports = de;
});

// node_modules/cross-spawn/lib/enoent.js
var yt = l((Le, bt) => {
  "use strict";
  var k = process.platform === "win32";
  function T(t, e) {
    return Object.assign(new Error(`${e} ${t.command} ENOENT`), {
      code: "ENOENT",
      errno: "ENOENT",
      syscall: `${e} ${t.command}`,
      path: t.command,
      spawnargs: t.args
    });
  }
  function fe(t, e) {
    if (!k)
      return;
    let n = t.emit;
    t.emit = function(r, s) {
      if (r === "exit") {
        let o = xt(s, e, "spawn");
        if (o)
          return n.call(t, "error", o);
      }
      return n.apply(t, arguments);
    };
  }
  function xt(t, e) {
    return k && t === 1 && !e.file ? T(e.original, "spawn") : null;
  }
  function me(t, e) {
    return k && t === 1 && !e.file ? T(e.original, "spawnSync") : null;
  }
  bt.exports = {
    hookChildProcess: fe,
    verifyENOENT: xt,
    verifyENOENTSync: me,
    notFoundError: T
  };
});

// node_modules/cross-spawn/index.js
var Ot = l((je, E) => {
  "use strict";
  var _t = require("child_process"), A = wt(), R = yt();
  function Pt(t, e, n) {
    let r = A(t, e, n), s = _t.spawn(r.command, r.args, r.options);
    return R.hookChildProcess(s, r), s;
  }
  function he(t, e, n) {
    let r = A(t, e, n), s = _t.spawnSync(r.command, r.args, r.options);
    return s.error = s.error || R.verifyENOENTSync(s.status, r), s;
  }
  E.exports = Pt;
  E.exports.spawn = Pt;
  E.exports.sync = he;
  E.exports._parse = A;
  E.exports._enoent = R;
});

// src/main.ts
var xe = {};
Ht(xe, {
  ExecProcess: () => b,
  exec: () => $t,
  x: () => Rt
});
module.exports = zt(xe);
var Ct = require("child_process"), St = require("path"), kt = require("process");

// src/env.ts
var m = require("path"), Wt = /^path$/i, q = { key: "PATH", value: "" };
function Dt(t) {
  for (let e in t) {
    if (!Object.prototype.hasOwnProperty.call(t, e) || !Wt.test(e))
      continue;
    let n = t[e];
    return n ? { key: e, value: n } : q;
  }
  return q;
}
function Kt(t, e) {
  let n = e.value.split(m.delimiter), r = t, s;
  do
    n.push((0, m.resolve)(r, "node_modules", ".bin")), s = r, r = (0, m.dirname)(r);
  while (r !== s);
  return { key: e.key, value: n.join(m.delimiter) };
}
function I(t, e) {
  let n = {
    ...process.env,
    ...e
  }, r = Kt(t, Dt(n));
  return n[r.key] = r.value, n;
}

// src/stream.ts
var L = require("stream");
var j = (t) => {
  let e = t.length, n = new L.PassThrough(), r = () => {
    --e === 0 && n.emit("end");
  };
  for (let s of t)
    s.pipe(n, { end: !1 }), s.on("end", r);
  return n;
};

// src/main.ts
var Tt = N(require("readline"), 1), At = N(Ot(), 1), ge = {
  timeout: void 0,
  persist: !1
}, Ee = {
  windowsHide: !0
};
function ve(t, e) {
  return {
    command: (0, St.normalize)(t),
    args: e ?? []
  };
}
function we(t) {
  let e = new AbortController();
  for (let n of t) {
    if (n.aborted)
      return e.abort(), n;
    let r = () => {
      e.abort(n.reason);
    };
    n.addEventListener("abort", r, {
      signal: e.signal
    });
  }
  return e.signal;
}
var b = class {
  _process;
  _aborted = !1;
  _options;
  _command;
  _args;
  _resolveClose;
  _processClosed;
  _thrownError;
  get process() {
    return this._process;
  }
  get pid() {
    return this._process?.pid;
  }
  get exitCode() {
    if (this._process && this._process.exitCode !== null)
      return this._process.exitCode;
  }
  constructor(e, n, r) {
    this._options = {
      ...ge,
      ...r
    }, this._command = e, this._args = n ?? [], this._processClosed = new Promise((s) => {
      this._resolveClose = s;
    });
  }
  kill(e) {
    return this._process?.kill(e) === !0;
  }
  get aborted() {
    return this._aborted;
  }
  get killed() {
    return this._process?.killed === !0;
  }
  pipe(e, n, r) {
    return $t(e, n, {
      ...r,
      stdin: this
    });
  }
  async *[Symbol.asyncIterator]() {
    let e = this._process;
    if (!e)
      return;
    let n = [];
    this._streamErr && n.push(this._streamErr), this._streamOut && n.push(this._streamOut);
    let r = j(n), s = Tt.default.createInterface({
      input: r
    });
    for await (let o of s)
      yield o.toString();
    if (await this._processClosed, e.removeAllListeners(), this._thrownError)
      throw this._thrownError;
  }
  async _waitForOutput() {
    let e = this._process;
    if (!e)
      throw new Error("No process was started");
    let n = "", r = "";
    if (this._streamErr)
      for await (let o of this._streamErr)
        n += o.toString();
    if (this._streamOut)
      for await (let o of this._streamOut)
        r += o.toString();
    if (await this._processClosed, this._options?.stdin && await this._options.stdin, e.removeAllListeners(), this._thrownError)
      throw this._thrownError;
    return {
      stderr: n,
      stdout: r
    };
  }
  then(e, n) {
    return this._waitForOutput().then(e, n);
  }
  _streamOut;
  _streamErr;
  spawn() {
    let e = (0, kt.cwd)(), n = this._options, r = {
      ...Ee,
      ...n.nodeOptions
    }, s = [];
    this._resetState(), n.timeout !== void 0 && s.push(AbortSignal.timeout(n.timeout)), n.signal !== void 0 && s.push(n.signal), n.persist === !0 && (r.detached = !0), s.length > 0 && (r.signal = we(s)), r.env = I(e, r.env);
    let { command: o, args: i } = ve(this._command, this._args), u = (0, At._parse)(o, i, r), c = (0, Ct.spawn)(
      u.command,
      u.args,
      u.options
    );
    if (c.stderr && (this._streamErr = c.stderr), c.stdout && (this._streamOut = c.stdout), this._process = c, c.once("error", this._onError), c.once("close", this._onClose), n.stdin !== void 0 && c.stdin && n.stdin.process) {
      let { stdout: a } = n.stdin.process;
      a && a.pipe(c.stdin);
    }
  }
  _resetState() {
    this._aborted = !1, this._processClosed = new Promise((e) => {
      this._resolveClose = e;
    }), this._thrownError = void 0;
  }
  _onError = (e) => {
    if (e.name === "AbortError" && (!(e.cause instanceof Error) || e.cause.name !== "TimeoutError")) {
      this._aborted = !0;
      return;
    }
    this._thrownError = e;
  };
  _onClose = () => {
    this._resolveClose && this._resolveClose();
  };
}, Rt = (t, e, n) => {
  let r = new b(t, e, n);
  return r.spawn(), r;
}, $t = Rt;
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ExecProcess,
  exec,
  x
});
